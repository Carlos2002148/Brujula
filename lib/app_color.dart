import 'package:flutter/material.dart';

class AppColor {
  // Fondo principal (Asfalto/Cielo nocturno)
  static const Color primaryColor = Color(0xFF121417);

  // Marco de la brújula (Gris Industrial)
  static const Color PrimaryDarkColor = Color(0xFF1E1E1E);

  // Círculo central (Azul Neón Urbano)
  static const Color greenColor = Color(0xFF00B0FF);
  static const Color greenDarkColor = Color(0xFF0081CB);

  // Detalles
  static const Color white = Color(0xFFF5F5F5);
  static const Color black = Color(0xFF000000);
  static const Color red = Color(0xFFFF3D00); // Rojo anaranjado más moderno
  static const Color grey = Color(0xFF9E9E9E);

  // Color de texto secundario
  static const Color lightGrey = Color(0xFFBDBDBD);
}